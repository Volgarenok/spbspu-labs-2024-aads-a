#ifndef CMDS_HPP
#define CMDS_HPP

#include "AVLTree.hpp"

namespace skuratov
{
  void isPrint(std::istream& in, std::ostream& out);
  void isComplement(std::istream& in);
  void isIntersect(std::istream& in);
  void isUnion( std::istream& in);
}

#endif
